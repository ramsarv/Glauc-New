# ══════════════════════════════════════════════════════
# Glauc  requirements.txt  v3.0
# pip install -r requirements.txt
# ══════════════════════════════════════════════════════

# ── Core ML ────────────────────────────────────────────
torch>=2.3.0
torchvision>=0.18.0
numpy>=1.26.0
Pillow>=10.3.0
scipy>=1.13.0            # used by calibration (_conf_to_z)

# ── DINOv3 ─────────────────────────────────────────────
# Loaded via torch.hub.load() — no pip install needed.
# Weights auto-download on first run (~330MB for vitb14).

# ── Qwen3-VL ───────────────────────────────────────────
# Install latest transformers from source (required for Qwen3-VL):
#   pip install git+https://github.com/huggingface/transformers.git
transformers>=4.47.0
accelerate>=0.34.0
qwen-vl-utils>=0.0.10

# Optional — Flash Attention 2 (40-60% speedup on CUDA A100/H100):
#   pip install flash-attn --no-build-isolation

# Optional — INT4 quantization (Qwen3-VL VRAM: 16GB → 5GB):
#   pip install auto-gptq optimum
#   Then add load_in_4bit=True to GlaucExplainer.from_pretrained()

# ── Eye cropping ───────────────────────────────────────
mediapipe>=0.10.14
opencv-python-headless>=4.10.0   # headless: no GUI deps, safe for servers

# ── Visualisation ──────────────────────────────────────
matplotlib>=3.9.0

# ── API server ─────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# ── Testing / dev ──────────────────────────────────────
requests>=2.32.0

# ══════════════════════════════════════════════════════
# OUTPUT FILES GENERATED BY THIS PIPELINE
# ══════════════════════════════════════════════════════
# glauc_outputs/
#   best_model.pt              DINOv3 checkpoint (temperature calibration included)
#   vocab.json                 demographic vocabulary
#   training_curves.png        Huber loss + MAE + LR schedule
#   prediction_analysis.png    scatter with CI bars, error hist, CI vs age
#   demographic_breakdown.png  MAE by gender and race
#   risk_scores.png            disease head distributions
#   gradcam_grid.png           GradCAM attention heatmaps (12 val images)
#   reliability_diagram.png    calibration coverage plot + ECE
#   calibration_summary.json   ECE + temperature values
#   bias_audit.txt             subgroup fairness report
#   predictions.csv            per-image: age, CI, risk scores, explanation
#   explanations_report.txt    Qwen3-VL clinical explanations
#   training_summary.txt       full run config + metrics
#   glauc_predictions.db       SQLite longitudinal prediction store

# ══════════════════════════════════════════════════════
# HARDWARE REQUIREMENTS
# ══════════════════════════════════════════════════════
# Component              VRAM
# ─────────────────────  ──────────
# DINOv3 ViT-B/14        ~4 GB
# Qwen3-VL-8B (fp16)    ~16 GB
# Qwen3-VL-8B (int4)     ~5 GB    ← use auto-gptq
# Both (fp16)           ~20 GB    ← A100 40GB recommended
# Both (int4)           ~10 GB    ← RTX 4090 24GB works
#
# TTA adds ~8× inference time per image (still <5s on A100).
# For latency-sensitive deployments: set TTA_VIEWS=4 or use_tta=False.
#
# Recommended cloud GPU
#   Dev / pilot   : RTX 4090 (24GB) or A10G (24GB)
#   Production    : A100 (40GB) — fits both models at fp16 with headroom
#   Budget option : RunPod or Vast.ai (A100 ~$1-2/hr)
#   CPU fallback  : works but ~15-45s per prediction — not production-ready
